#include <tunables/global>

profile hassio_example flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities required for the add-on
  capability net_bind_service,
  capability dac_override,

  # Network access (FastAPI backend + WebSocket to HA)
  network,

  # Allow read access to the whole filesystem (dev-friendly, restrict in production)
  /usr/src/** r,
  /usr/local/lib/python3*/** r,
  /usr/local/bin/* rix,
  /bin/** rix,
  /usr/bin/** rix,

  # Mapped volumes
  /share/** rw,

  # Temp & runtime files
  /tmp/** rw,
  /run/** rw,

  # Python / Node executables
  /usr/local/bin/python3* rix,
  /usr/bin/node rix,
  /usr/local/bin/node rix,
}
